dependencies:
  cache_directories:
    - "~/.meteor"

  override:
    - meteor || curl https://install.meteor.com | /bin/sh

test:
  override:
    - cd app && meteor --test --once --settings ../config/development/settings.json
